import React from 'react';

const FormInput = (props) => {
  const {
    column,
    handleSourceChange,
  } = props;

  return (
    <div className={column}>
      FORMINPUT
    </div>
  )
};

export default FormInput;