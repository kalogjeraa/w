import React from 'react';

const FormInput = (props) => {
  const {
    column,
    handleFormChange,
  } = props;

  return (
    <div className={column}>
      FORMINPUT
    </div>
  )
};

export default FormInput;