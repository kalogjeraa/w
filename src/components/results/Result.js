import React from 'react';

const Result = (props) => {
  const {
    column,
    result,
  } = props;

  return (
    <div className={column}>
      RESULT
      
    </div>
  )
};

export default Result;